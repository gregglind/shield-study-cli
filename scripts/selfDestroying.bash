set -x
set -o nounset
set -o errexit

# copy over the template to ... somewhere
# modify stuff in a the package.json?  Start from a package.json if one is given?
# use the ID in particular
# increament OR give the version?
